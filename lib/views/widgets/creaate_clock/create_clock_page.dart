import 'package:flutter/material.dart';

class CreateClockPage extends StatelessWidget {
  const CreateClockPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
