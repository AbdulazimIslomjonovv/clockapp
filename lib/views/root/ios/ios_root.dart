import 'package:flutter/material.dart';

class IosRoot extends StatelessWidget {
  const IosRoot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
